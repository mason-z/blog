{
  "name": "tony-hexo-blog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prevalidate": "node tools/sync-admin-config.cjs",
    "validate": "node tools/check-repo-health.cjs",
    "sync:admin": "node tools/sync-admin-config.cjs",
    "build": "npm run validate && hexo generate -f --config _config.yml,_config.local.yml && npm run sync:admin",
    "build:prod": "npm run validate && hexo generate -f && npm run sync:admin",
    "site": "npm run build",
    "gen": "npm run build",
    "clean": "hexo clean",
    "server": "hexo server -p 4001 --config _config.yml,_config.local.yml",
    "server:4001": "npm run server",
    "start": "npm run server",
    "dev": "npm run server",
    "preview": "hexo server -p 4001 --config _config.yml,_config.local.yml",
    "preview:local": "npm run build && npm run preview",
    "serve:public": "http-server public -p 4002 -c-1",
    "cms:proxy": "cross-env MODE=git decap-server",
    "dev:admin": "concurrently -k -n proxy,hexo -c cyan,magenta \"npm run cms:proxy\" \"npm run server:4001\"",
    "gen:demo": "node tools/gen-demo-posts.cjs"
  },
  "hexo": {
    "version": "7.3.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "decap-server": "^3.6.0",
    "http-server": "^14.1.1",
    "js-yaml": "^4.1.0"
  },
  "dependencies": {
    "hexo": "^7.3.0",
    "hexo-generator-archive": "^2.0.0",
    "hexo-generator-category": "^2.0.0",
    "hexo-generator-index": "^3.0.0",
    "hexo-generator-tag": "^2.0.0",
    "hexo-renderer-ejs": "^2.0.0",
    "hexo-renderer-marked": "^6.3.0",
    "hexo-server": "^3.0.0"
  }
}
