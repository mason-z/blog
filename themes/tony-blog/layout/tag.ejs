<section class="archive-shell home-shell">
  <% var _uily = (site.data.site_ui && site.data.site_ui.labels) || {}; %>
  <h1 class="page-title"><%= _uily.tag_page_title_prefix || '标签：' %><%= page.tag %></h1>
  <div class="home-grid">
    <div class="home-main">
      <div class="post-list post-list--home">
        <% page.posts.each(function(post){ %>
          <%- partial('_partial/post-card', { post: post }) %>
        <% }); %>
      </div>
      <%- partial('_partial/pagination', { page: page }) %>
    </div>
  </div>
</section>
